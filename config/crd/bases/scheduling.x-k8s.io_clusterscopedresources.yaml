---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: clusterscopedresources.scheduling.x-k8s.io
spec:
  group: scheduling.x-k8s.io
  names:
    kind: ClusterScopedResource
    listKind: ClusterScopedResourceList
    plural: clusterscopedresources
    singular: clusterscopedresource
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterScopedResource is a collection of cluster scoped resources
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Specification of the desired behavior of the cluster scoped
              resource.
            properties:
              resourcesFilter:
                additionalProperties:
                  type: boolean
                type: object
              resourcesScore:
                additionalProperties:
                  format: int64
                  type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
